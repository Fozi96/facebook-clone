<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
  <body>
  <% flash.each do |key, value| %>
   <%= content_tag(:div, value, class: "#{key}") %>
   <% end %>
   <% if logged_in? %>
   <%= link_to "sign up", user_path(current_user.id) %>
   <%= link_to "Login", session_path(current_user.id), method: :delete %>
   <% else %>
   <%= link_to "Profil", new_user_path %>
   <%= link_to "Logout", new_session_path %>
  <% end %>
  <%= yield %>
    <footer>
      <p> Operating company DIVE INTO CODE Co., Ltd. </ P>
      <p>© 2015-2016 DIVE INTO CODE Corp.</p>
    </footer>
  </body>
  </html>
